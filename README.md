# alex-net-viz
Visualization of the Layers of the Alex Net implementation from Pytorch.


## How to run

Run `python3 app.py`, that will start the server that will run the alex net. You may need to install some python dependencies.

Then you can open the `index.html` on your browser.
